package ${config.packageName}.dao.${config.module};

import org.webbuilder.web.core.dao.GenericMapper;
import ${config.packageName}.po.${config.module}.${config.className};

/**
* ${config.remark!''}数据映射接口
* Created by generator ${create_time!''}
*/
public interface ${config.className}Mapper extends GenericMapper<${config.className},String> {

}
